// Class-name strings for the Tarefas wrappers. Styling lives in the plain global
// stylesheet tf-theme.css (imported in app/tarefas/page.tsx). `tf-scope` carries
// the design's CSS-variable bridge; `tf-root`/`tf-inline` are layout.
export const tfRoot = "tf-root tf-scope"
export const tfInline = "tf-inline tf-scope"
