// Class-name strings for the CRM wrappers. Styling lives in the plain global
// stylesheet crm-theme.css. `crm-scope` carries the design's CSS-variable
// bridge; `crm-root`/`crm-inline` are layout. Mirrors comercial/cm-classes.ts.
export const crmRoot = "crm-root crm-scope"
export const crmInline = "crm-inline crm-scope"
export const crmScope = "crm-scope"
