// Class-name strings for the Comercial wrappers. Styling lives in the plain
// global stylesheet cm-theme.css (imported in app/comercial/page.tsx). `cm-scope`
// carries the design's CSS-variable bridge; `cm-root`/`cm-inline` are layout.
export const cmRoot = "cm-root cm-scope"
export const cmInline = "cm-inline cm-scope"
