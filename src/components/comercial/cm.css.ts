// Deprecated. The Comercial styling moved to the plain global stylesheet
// `cm-theme.css` (imported in app/comercial/page.tsx) + `cm-classes.ts`, because
// vanilla-extract `globalStyle` from a client-imported `.css.ts` wasn't being
// emitted in this Next setup. This file is intentionally empty.
export {}
